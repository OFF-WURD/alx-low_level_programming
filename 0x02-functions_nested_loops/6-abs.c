#include "main.h"
/**
* _abs - it return the absolute value of a number
* @n: integer to be tested
* Return: void.
*/
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}

}
